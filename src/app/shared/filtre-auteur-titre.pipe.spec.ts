import { FiltreAuteurTitrePipe } from './filtre-auteur-titre.pipe';

describe('FiltreAuteurTitrePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreAuteurTitrePipe();
    expect(pipe).toBeTruthy();
  });
});
